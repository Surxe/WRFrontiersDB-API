services:
  - type: web
    name: game-data-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/run.py
    envVars:
      - key: PORT
        value: 10000
